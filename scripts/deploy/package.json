{
  "name": "deploy",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "esbuild index.ts --bundle  --platform=node --outfile=app.js",
    "publish": "esbuild index.ts --bundle  --platform=node --outfile=app.js && node app.js"
  },
  "dependencies": {
    "esbuild": "^0.8.44",
    "mkdirp": "^1.0.4",
    "showdown": "^1.9.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.25",
    "@types/showdown": "^1.9.3"
  }
}
